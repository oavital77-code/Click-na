[
  {
    "advisory": "GHSA-3f6p-5ww8-9rcr",
    "package": "mysql2",
    "reason": "MySQL driver bundled inside the prisma CLI, a devDependency. This project is PostgreSQL-only (@prisma/adapter-pg) and never loads mysql2; it is not part of the deployed runtime. The offered fix downgrades prisma to 6.x, which drops the driver-adapter API this app is built on.",
    "reviewBy": "2026-12-01"
  },
  {
    "advisory": "GHSA-ggr8-5vv4-36mx",
    "package": "deepmerge-ts",
    "reason": "Reached only through @prisma/config when the prisma CLI (devDependency) parses our own prisma.config.ts. Triggering the stack exhaustion requires a maliciously recursive config file, which is a file we author. Same downgrade problem as above.",
    "reviewBy": "2026-12-01"
  }
]
